{
  "name": "hedge-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others \"cd frontend && npm start\" \"cd backend && python bot.py\"",
    "install": "cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
